projects:
  - name: "pythonutil"
    environments:
      - name: "Development"
        slug: "dev"
        configs:
          - slug: "dev"
          - slug: "dev_siggi"
    secrets:
      dev:
        EMAILSERVER: "${shared.dev.EMAILSERVER}"
        EMAILUSER: "${shared.dev.EMAILUSER}"
        EMAILPWD: "${shared.dev.EMAILPWD}"